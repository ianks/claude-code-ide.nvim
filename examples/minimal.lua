-- Minimal configuration for claude-code.nvim
-- The absolute minimum needed to get started

return {
	"ianks/claude-code.nvim",
	dependencies = {
		"nvim-lua/plenary.nvim",
	},
	config = true, -- Use default setup
}
